# ghcr-pull-secret ESO — sync-wave -1 so the pull secret is available before the Deployment is created.
# Backed by secret/terminus/default/ghcr/pull-token in Vault (shared across all services).
# See cicd.md: "Every GHCR Consumer Chart Must Create ghcr-pull-secret"
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ghcr-pull-secret-eso
  namespace: {{ .Values.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  labels:
    {{- include "terminus-watchdog.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: ghcr-pull-secret
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: '{"auths":{"ghcr.io":{"username":"electricm0nk","password":"{{ "{{" }} .token {{ "}}" }}","auth":"{{ "{{" }} list "electricm0nk" .token | join ":" | b64enc {{ "}}" }}"}}}'
  data:
    - secretKey: token
      remoteRef:
        key: secret/terminus/default/ghcr/pull-token
        property: token
